apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mycode-pvc-dynamic
  namespace: hs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: local-path
